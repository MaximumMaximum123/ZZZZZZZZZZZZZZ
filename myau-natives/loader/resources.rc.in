// Generated by CMake. The payload lives inside the executable so that there is
// one file to hand round instead of three -- and so the jar can never be a
// different build from the DLL that expects it.
#include "resource.h"

IDR_NATIVE_DLL RCDATA "@PAYLOAD_DIR@/myau_native.dll"
IDR_CLIENT_JAR RCDATA "@PAYLOAD_DIR@/client.jar"
@LOGO_RESOURCE@
@ICON_RESOURCE@
